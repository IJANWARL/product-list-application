import productsDetails from 'containers/ProductDetails/duck/reducer';
import productsList from 'containers/ProductsList/duck/reducer';

const reducer = {
  productsList,
  productsDetails
};

export default reducer;
